# PostgreSQL database connection string
DATABASE_URL="postgresql://user:password@host:port/dbname?sslmode=disable"

# Base URL for the transaction-service API
TRANSACTION_SERVICE_URL="http://localhost:8083"

# The amount in kobo for a monthly subscription (1000 = ₦10.00)
SUBSCRIPTION_FEE_KOBO=1000

# Cron schedule for the monthly subscription billing job.
# Default: "0 2 1 * *" (At 02:00 on day-of-month 1.)
# Format: "minute hour day-of-month month day-of-week"
BILLING_JOB_SCHEDULE="0 2 1 * *"

# Cron schedule for resetting the monthly transfer usage count.
# Default: "0 1 1 * *" (At 01:00 on day-of-month 1.)
# Format: "minute hour day-of-month month day-of-week"
RESET_USAGE_JOB_SCHEDULE="0 1 1 * *"

# Optional: Timezone for cron jobs (defaults to UTC)
# TZ="Africa/Lagos"
